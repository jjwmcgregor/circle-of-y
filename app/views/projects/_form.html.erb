<%= form_with(model: project, url:[@user, @project], local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :intro %>
    <%= form.text_area :intro %>
  </div>

  <div class="field">
    <%= form.label :impact %>
    <%= form.text_area :impact %>
  </div>

  <div class="field">
    <%= form.label :businesscase %>
    <%= form.text_area :businesscase %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
